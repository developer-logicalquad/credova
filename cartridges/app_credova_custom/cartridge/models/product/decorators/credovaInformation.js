'use strict';
var prodAttributeModel = require('dw/catalog/ProductAttributeModel');
module.exports = function (product, apiProduct, options) {
    // var credovaEnabledListing =  apiProduct.attributeModel.getValue(apiProduct.attributeModel.attributeGroups[10].attributeDefinitions[0]);
    // var credovaDisableCheckout = product.credovaDisableCheckout;
    // Object.defineProperty(product, 'credovaEnabledListing', {
    //     enumerable: true,
    //     value: credovaEnabledListing == null ? 0 : parseInt(credovaEnabledListing.ATS, 10)
    // });
    // Object.defineProperty(product, 'credovaDisableCheckout', {
    //     enumerable: true,
    //     value: credovaDisableCheckout == null ? 0 : parseInt(credovaDisableCheckout.ATS, 10)
    // });
};
